<?xml version="1.0" encoding="utf-8"?>
<!-- The APK shim: pure NativeActivity, zero Java (hasCode=false) — the app is
     libinteraction_combinators.so. Per okf/concepts/substrates.md, platform
     shims are build scaffolding with no logic in them. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.voidmaiz.combinators">

  <uses-feature android:glEsVersion="0x00030000" android:required="true" />

  <!-- LAN collaboration (VoidMaiz okf/concepts/collaborative-canvas.md, E25;
       the list is maiz::lan::android_manifest_permissions). All four are
       "normal" permissions: granted at install, no runtime prompt, no Java,
       so hasCode=false still holds. CHANGE_WIFI_MULTICAST_STATE is exercised
       only if the app opts into maiz::lan::MulticastLock (phone-to-phone
       discovery). -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

  <application
      android:label="Interaction Combinators"
      android:hasCode="false"
      android:extractNativeLibs="true"
      android:theme="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen">
    <activity
        android:name="android.app.NativeActivity"
        android:exported="true"
        android:screenOrientation="portrait"
        android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density|uiMode">
      <meta-data android:name="android.app.lib_name"
                 android:value="interaction_combinators" />
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
