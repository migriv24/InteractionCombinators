<?xml version="1.0" encoding="utf-8"?>
<!-- The APK shim: a NativeActivity plus ONE Java class, Void Maiz's
     org.voidmaiz.MaizActivity, which is what lets Android's own keyboard type
     into the app (VoidMaiz okf/concepts/text-input.md, 2026-09-23). The app is
     still libinteraction_combinators.so, and the Java holds no logic.
     adjustNothing: the keyboard must not pan or resize the native surface. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.voidmaiz.combinators">

  <uses-feature android:glEsVersion="0x00030000" android:required="true" />

  <!-- LAN collaboration (VoidMaiz okf/concepts/collaborative-canvas.md, E25;
       the list is maiz::lan::android_manifest_permissions). All four are
       "normal" permissions: granted at install, no runtime prompt, no Java,
       and no Java of their own. CHANGE_WIFI_MULTICAST_STATE is exercised
       only if the app opts into maiz::lan::MulticastLock (phone-to-phone
       discovery). -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
  <!-- Updating itself (VoidMaiz okf/concepts/updates.md): hand a verified APK to
       Android's PackageInstaller, which always asks the person to confirm.
       The person also allows installs from this app, once. -->
  <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

  <application
      android:label="Interaction Combinators"
      android:hasCode="true"
      android:extractNativeLibs="true"
      android:theme="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen">
    <activity
        android:name="org.voidmaiz.MaizActivity"
        android:windowSoftInputMode="adjustNothing"
        android:exported="true"
        android:screenOrientation="fullUser"
        android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density|uiMode">
      <meta-data android:name="android.app.lib_name"
                 android:value="interaction_combinators" />
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
